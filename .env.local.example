# Agentronics landing — environment variables
# Copy to .env.local. The site BOOTS WITHOUT real values (auth is stubbed for now).

# --- Clerk (auth) — wired in a later phase ---
# Get real keys from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_TODO
CLERK_SECRET_KEY=sk_test_TODO

# --- Backend (later phase) ---
# Neon Postgres connection string
# DATABASE_URL=postgres://...
# Google Cloud Run API base (gateway / scheduling agent)
# NEXT_PUBLIC_API_BASE_URL=https://...
